{
  inputStage = SnapInputStage

  SnapInputStage =
  {
    insertSizeRangeHigh = 2000
    dontUseBigPages = true
    unpairedMaxHitsPerRead = 2000
    pairedMaxHitsPerRead = 20000
  }
  assembler = 
  {
  }
  defPart = 
  {
  }
  bqsr =
  {
  }

  preprocessorNames = ( bqsr );
  preprocessorAlgorithms = ( recalibrateBaseQualities );

  partitionerNames = ( defPart );
  partitionerAlgorithms = ( defaultPartitioner );

  callNames = ( assembler );
  callAlgorithms = ( AssemblyPhaser );

  postprocessorNames = ( );
  postprocessorAlgorithms = ( );
}
